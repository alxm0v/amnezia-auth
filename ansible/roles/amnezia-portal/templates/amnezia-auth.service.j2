[Unit]
Description=AmneziaWG Captive Portal Auth Service
After=network.target

[Service]
User=root
WorkingDirectory=/opt/amnezia-auth
ExecStart=/opt/amnezia-auth/venv/bin/uvicorn main:app --host 0.0.0.0 --port {{ captive_portal_port }}
Restart=always

[Install]
WantedBy=multi-user.target
