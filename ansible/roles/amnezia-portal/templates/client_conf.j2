[Interface]
# REPLACE WITH YOUR PRIVATE KEY!
PrivateKey = REPLACE_ME_WITH_YOUR_PRIVATE_KEY
Address = {{ item.ip }}/32

[Peer]
# AmneziaWG Captive Portal Server
PublicKey = {{ vault.server_public_key }}
Endpoint = {{ vpn_public_ip_or_domain }}:{{ vpn_port }}
AllowedIPs = {{ vpn_server_ip }}/32, {{ allowed_subnets | join(', ') }}
PersistentKeepalive = 25

# AmneziaWG Obfuscation Parameters
{{ awg_obfuscation_settings }}
